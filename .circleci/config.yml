version: 2
jobs:
  build_windows:
    docker:
      - image: electronuserland/builder:wine
    steps:
      - checkout
      - run: yarn && yarn release --win --publish=never

workflows:
  version: 2
  build:
    jobs:
      - build_windows
